Peter's personal website. Hosted on GitHub Pages.

## Local development

Use Ruby 3.2+ with Bundler 2.7.2.

- Install dependencies: `bundle install`
- Run locally: `bundle exec jekyll serve`
- Verify the build: `bundle exec jekyll build`
